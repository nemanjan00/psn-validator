const https = require('https');

https.get('https://my.playstation.com/king', (res) => {
	var response = '';

	res.on('data', (data) => {
		response += data;
	});

	res.on('end', (d) => {
		console.log(response.contains("avatar"));
	});
}).on('error', (e) => {
	console.error(e);
});
